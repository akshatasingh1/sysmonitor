"""Structured logging setup.

Implemented in Phase 4: a ``get_logger()`` that attaches a
``RotatingFileHandler`` and a console ``StreamHandler``, with ``text`` and
``json`` formatters selectable from config.
"""
